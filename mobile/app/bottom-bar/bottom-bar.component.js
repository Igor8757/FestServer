angular.
module('bottomBar').
component('bottomBar', {
  // Note: The URL is relative to our `index.html` file
  templateUrl: 'app/bottom-bar/bottom-bar.template.html',
  controller: function bottomBarController() {
            
        }
});