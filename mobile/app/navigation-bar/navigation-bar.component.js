angular.
module('navigationBar').
component('navigationBar', {
  // Note: The URL is relative to our `index.html` file
  templateUrl: 'app/navigation-bar/navigation-bar.template.html',
  controller: function navigationBarController() {
            
        }
});