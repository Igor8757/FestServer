angular.
module('buyForm').
component('buyForm', {
  // Note: The URL is relative to our `index.html` file
  templateUrl: 'app/buy-form/buy-form.template.html',
  controller: function buyFormController($http) {
    
  }
});